rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // In development, temporary open rules. It is recommended to restrict this later.
      allow read, write: if true;
    }
  }
}
